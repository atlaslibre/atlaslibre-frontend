export interface ModelParams {
  filename: string;
  scale: number;
  minSize: number;
  maxSize: number;
  rotation: number;
}
export const jetModel: ModelParams = {
  filename: "jet.glb",
  scale: 1000,
  minSize: 1.5,
  maxSize: 3,
  rotation: 180,
};

export const buoyModel: ModelParams = {
    filename: "buoy.glb",
    scale: 100,
    minSize: 1.75,
    maxSize: 10,
    rotation: 180,
  };
  
export const tankerModel: ModelParams = {
  filename: "tanker.glb",
  scale: 15,
  minSize: 0.1,
  maxSize: 1,
  rotation: 180,
};

export const cargoModel: ModelParams = {
    filename: "cargo.glb",
    scale: 15,
    minSize: 0.1,
    maxSize: 1,
    rotation: 180,
  };
  
  
export const containerModel: ModelParams = {
    filename: "container.glb",
    scale: 15,
    minSize: 0.1,
    maxSize: 1,
    rotation: 180,
  };
  
  export const militaryModel: ModelParams = {
    filename: "military.glb",
    scale: 15,
    minSize: 0.1,
    maxSize: 1,
    rotation: 180,
  };
  
  export const recreationalModel: ModelParams = {
    filename: "recreational.glb",
    scale: 30,
    minSize: 0.1,
    maxSize: 1,
    rotation: 180,
  };
  
  export const ferryModel: ModelParams = {
    filename: "ferry.glb",
    scale: 15,
    minSize: 0.1,
    maxSize: 1,
    rotation: 180,
  };
  
  export const specialModel: ModelParams = {
    filename: "special.glb",
    scale: 15,
    minSize: 0.1,
    maxSize: 1,
    rotation: 180,
  };
  export const highspeedModel: ModelParams = {
    filename: "highspeed.glb",
    scale: 30,
    minSize: 0.1,
    maxSize: 1,
    rotation: 180,
  };
  
  export const duckModel: ModelParams = {
    filename: "duck.glb",
    scale: 100,
    minSize: 1.75,
    maxSize: 10,
    rotation: 0,
  };